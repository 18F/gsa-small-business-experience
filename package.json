{
  "name": "gsa-small-business-experience",
  "version": "0.0.1",
  "scripts": {
    "build": "bundle exec jekyll build",
    "clean": "bundle exec jekyll clean",
    "reset": "npx rimraf .git",
    "start": "bundle exec jekyll serve --livereload",
    "start-detached": "bundle exec jekyll serve --detach",
    "htmlproofer": "bundle exec htmlproofer _site",
    "pa11y-ci:sitemap": "pa11y-ci --sitemap http://localhost:4000/sitemap.xml --sitemap-exclude \"/*.pdf\"  --sitemap-exclude \"/admin\"",
    "test": "npm run woke; npm run htmlproofer; npm run pa11y-ci:sitemap",
    "test-build": "npm run htmlproofer; npm run pa11y-ci:sitemap",
    "woke": "woke"
  },
  "dependencies": {
    "netlify-cms": "^2.10.186",
    "uswds": "^2.13.1"
  },
  "devDependencies": {
    "pa11y-ci": "^3.0.1",
    "rimraf": "^3.0.2"
  }
}
